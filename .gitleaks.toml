title = "LedgerGuard gitleaks config"

[extend]
useDefault = true

[[allowlists]]
description = "Placeholder tokens in README examples are not real secrets"
regexes = [
  '''YOUR_BOOTSTRAP_TOKEN''',
  '''YOUR_LOCAL_TOKEN''',
]
paths = [
  '''README.md''',
]
