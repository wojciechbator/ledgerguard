DATABASE_URL=postgres://ledgerguard:ledgerguard@localhost:5432/ledgerguard
LEDGERGUARD_BIND_ADDR=0.0.0.0:8080
RUST_LOG=ledgerguard=debug,tower_http=info

# Deliberately absent until the dedicated Saldeo API user exists.
# Never commit real credentials.
# SALDEO_USERNAME=
# SALDEO_API_TOKEN=
